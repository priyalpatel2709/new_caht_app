name: Flutter Web + APK Deploy

on:
  push:
    branches:
      - new_action

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.41.6"
          channel: stable

      - name: Flutter Pub Get
        run: flutter pub get

      # Build APK
      - name: Build APK
        run: flutter build apk --release

      # Build Web
      - name: Build Web
        run: flutter build web --release

      # Copy APK into website
      - name: Copy APK to Web Folder
        run: |
          cp build/app/outputs/flutter-apk/app-release.apk build/web/app.apk

      # Upload APK artifact
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk

      # Deploy to Netlify
      - name: Deploy To Netlify
        uses: nwtgck/actions-netlify@v3
        with:
          publish-dir: './build/web'
          production-branch: main
          github-token: ${{ secrets.GITHUB_TOKEN }}
          deploy-message: "Production Deploy"
          enable-pull-request-comment: false
          enable-commit-comment: false
          overwrites-pull-request-comment: false
        env:
          NETLIFY_AUTH_TOKEN: "nfp_rjnd3r5f4K81mhnrzyqh1AcfDpWh1qVP6c3a"
          NETLIFY_SITE_ID: "5b8e98c1-747f-46b0-bafe-142e5ebac37c"
